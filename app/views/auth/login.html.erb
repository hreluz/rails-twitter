<% if flash[:alert] %>
  <div class="alert">
    <%= flash[:alert] %>
  </div>
<% end %>

<h1>Login</h1>
<%= form_with model: @user, url: login_path do |form| %>
  <%= form.email_field :email %>
  <%= form.password_field :password %>
  <%= form.submit "Login Account" %>
<% end %>